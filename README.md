# Notes Generator

This repository contains the **AI Notes** Obsidian plugin. Build the plugin from
the `obsidian-ai-notes` directory using `make build` and copy it into your Vault
with `make install`. See the plugin README for details.
